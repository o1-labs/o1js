import { Property, Equivalent, ConstraintSystem } from '@o1js/testing';

// placeholder test
describe('@o1js/testing', () => {
  it('should export Testing namespace', () => {
    expect(Property).toBeDefined();
    expect(Equivalent).toBeDefined();
    expect(ConstraintSystem).toBeDefined();
  });
});
